# Copy to web/.env and fill in values from the Supabase project
# (Project Settings -> API). Placeholders ONLY — never commit real keys.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key